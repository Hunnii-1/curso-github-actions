name: Ejecutar Script de Bash

on: [push]

jobs:
  run_script:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout del repositorio
      uses: actions/checkout@v2

    - name: Ejecutar script Hola_mundo.sh
      run: bash ./Hola_mundo.sh
